io:format("aa~n").


%% shell:catch_exception(true).

%% set_env(App, Name, Val, Default) ->
%%     Prev = case application:get_env(App, Name) of
%%                undefined ->
%%                    Default;
%%                {ok, Old} ->
%%                    Old
%%     end,
%%     application_controller:set_env(App, Name, Val),
%%     Prev.
%%     
%% set_env(stdlib, shell_catch_exception, true, false).


application_controller:set_env(_App=stdlib,_Name=shell_catch_exception,true).
reloader:start().